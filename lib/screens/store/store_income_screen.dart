import 'package:flutter/material.dart';

class StoreIncomeScreen extends StatelessWidget {
  const StoreIncomeScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text(
        "รายได้ร้านค้า",
        style: TextStyle(fontSize: 24),
      ),
    );
  }
}
